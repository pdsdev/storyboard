:: We always use three command windows to do development
:: One is for "start-mongo", one is for "start-app" and one is for "build"
:: We don't automatically run the commands so we can stop and restart each tool as needed.
start cmd
start cmd
start cmd
